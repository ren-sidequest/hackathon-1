// Backward-compatible command name for the current default-contract process verifier.
// Historical API3 uses its original application revision and database, not remapped people.
await import('./verify-r6.mjs');
